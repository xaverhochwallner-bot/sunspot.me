# Flutter SDK (local install, not part of the project)
flutter/

# Python
__pycache__/
*.pyc
*.pyo

# VS Code
.vscode/

# Claude Code
.claude/

# Build outputs
build/

# Large OSM data files (raw — not in git)
*.osm.pbf
*.osm.bz2
# buildings_cache.pkl is tracked via Git LFS

# Contains credentials — never commit
provision_arm.py

# Local Netlify folder
.netlify

# Server-generated cache files (regenerated at startup)
sunspot_server/buildings_cache.pkl
sunspot_server/city_pois_cache.json
